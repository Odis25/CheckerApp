@page "/contracts"
@attribute [Authorize]

@inject HttpClient HttpClient
@inject NavigationManager NavigationManager


@if (ContractsList == null)
{
    <div class="spinner"></div>
}
else
{
    <h3>Список договоров</h3>
    <MatTable Items="@ContractsList.Contracts" Class="mat-elevation-z5" Striped="true" AllowSelection="true" OnRowDbClick="(item)=> NavigateTo(((ContractDetailVm)item).Id)">
        <MatTableHeader>
            <th scope="col">Id</th>
            <th scope="col">Название</th>
            <th scope="col">Номер договора</th>
            <th scope="col">Внутренний номер</th>
        </MatTableHeader>
        <MatTableRow>
            <td scope="col">@context.Id</td>
            <td scope="col">@context.Name</td>
            <td scope="col">@context.ContractNumber</td>
            <td scope="col">@context.DomesticNumber</td>
        </MatTableRow>
    </MatTable>
}

@code {

    ContractsListVm ContractsList { get; set; }

    protected override async Task OnInitializedAsync()
    {
        ContractsList = await HttpClient.GetFromJsonAsync<ContractsListVm>("api/contract");
    }

    private void NavigateTo(int id)
    {
        NavigationManager.NavigateTo($"/contract/{id}/detail");
    }
}
